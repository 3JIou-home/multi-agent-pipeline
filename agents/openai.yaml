interface:
  display_name: "Multi-Agent Pipeline"
  short_description: "Three-stage task orchestration"
  default_prompt: "Use $multi-agent-pipeline to turn a task into a staged multi-agent workflow."
